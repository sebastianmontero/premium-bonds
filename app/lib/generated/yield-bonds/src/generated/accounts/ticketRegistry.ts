/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  type Account,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from "@solana/accounts";
import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/codecs";

export const TICKET_REGISTRY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [58, 169, 167, 230, 107, 202, 126, 54]
);

export function getTicketRegistryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TICKET_REGISTRY_DISCRIMINATOR
  );
}

export type TicketRegistry = {
  discriminator: ReadonlyUint8Array;
  /** Pool ID this ticket registry belongs to. */
  poolId: number;
  /** Current user entry capacity — derived from account data_len() at init and each resize. */
  capacity: number;
  /** Total number of users registered in the pool. */
  userCount: number;
  /** Total active tickets owned by all users (participates in current drawing). */
  totalActiveTickets: number;
  /** Total pending tickets owned by all users (purchased during the current cycle, participates next cycle). */
  totalPendingTickets: number;
  /** The ID of the draw cycle currently being processed or the last completed cycle. */
  drawCycleId: number;
  /** Counter to keep track of draw preparation progress across batched crank transactions. */
  drawPreparedUpTo: number;
  /** Schema version of the struct. */
  version: number;
  /** Reserved space for future upgrades (96 bytes struct size total). */
  reserved: ReadonlyUint8Array;
};

export type TicketRegistryArgs = {
  /** Pool ID this ticket registry belongs to. */
  poolId: number;
  /** Current user entry capacity — derived from account data_len() at init and each resize. */
  capacity: number;
  /** Total number of users registered in the pool. */
  userCount: number;
  /** Total active tickets owned by all users (participates in current drawing). */
  totalActiveTickets: number;
  /** Total pending tickets owned by all users (purchased during the current cycle, participates next cycle). */
  totalPendingTickets: number;
  /** The ID of the draw cycle currently being processed or the last completed cycle. */
  drawCycleId: number;
  /** Counter to keep track of draw preparation progress across batched crank transactions. */
  drawPreparedUpTo: number;
  /** Schema version of the struct. */
  version: number;
  /** Reserved space for future upgrades (96 bytes struct size total). */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link TicketRegistryArgs} account data. */
export function getTicketRegistryEncoder(): FixedSizeEncoder<TicketRegistryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolId", getU32Encoder()],
      ["capacity", getU32Encoder()],
      ["userCount", getU32Encoder()],
      ["totalActiveTickets", getU32Encoder()],
      ["totalPendingTickets", getU32Encoder()],
      ["drawCycleId", getU32Encoder()],
      ["drawPreparedUpTo", getU32Encoder()],
      ["version", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 67)],
    ]),
    (value) => ({ ...value, discriminator: TICKET_REGISTRY_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link TicketRegistry} account data. */
export function getTicketRegistryDecoder(): FixedSizeDecoder<TicketRegistry> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolId", getU32Decoder()],
    ["capacity", getU32Decoder()],
    ["userCount", getU32Decoder()],
    ["totalActiveTickets", getU32Decoder()],
    ["totalPendingTickets", getU32Decoder()],
    ["drawCycleId", getU32Decoder()],
    ["drawPreparedUpTo", getU32Decoder()],
    ["version", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 67)],
  ]);
}

/** Gets the codec for {@link TicketRegistry} account data. */
export function getTicketRegistryCodec(): FixedSizeCodec<
  TicketRegistryArgs,
  TicketRegistry
> {
  return combineCodec(getTicketRegistryEncoder(), getTicketRegistryDecoder());
}

export function decodeTicketRegistry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<TicketRegistry, TAddress>;
export function decodeTicketRegistry<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<TicketRegistry, TAddress>;
export function decodeTicketRegistry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<TicketRegistry, TAddress> | MaybeAccount<TicketRegistry, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTicketRegistryDecoder()
  );
}

export async function fetchTicketRegistry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<TicketRegistry, TAddress>> {
  const maybeAccount = await fetchMaybeTicketRegistry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTicketRegistry<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<TicketRegistry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTicketRegistry(maybeAccount);
}

export async function fetchAllTicketRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<TicketRegistry>[]> {
  const maybeAccounts = await fetchAllMaybeTicketRegistry(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTicketRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<TicketRegistry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeTicketRegistry(maybeAccount)
  );
}

export function getTicketRegistrySize(): number {
  return 104;
}
