const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";

export const PREPARE_DRAW_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  1, 48, 179, 57, 145, 28, 26, 131,
]);

export function getPrepareDrawDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PREPARE_DRAW_DISCRIMINATOR
  );
}

export type PrepareDrawInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountCrank extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountDrawCycle extends string | AccountMeta<string> = string,
  TAccountTicketRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCrank extends string
        ? WritableSignerAccount<TAccountCrank> &
            AccountSignerMeta<TAccountCrank>
        : TAccountCrank,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountDrawCycle extends string
        ? WritableAccount<TAccountDrawCycle>
        : TAccountDrawCycle,
      TAccountTicketRegistry extends string
        ? WritableAccount<TAccountTicketRegistry>
        : TAccountTicketRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type PrepareDrawInstructionData = {
  discriminator: ReadonlyUint8Array;
  batchSize: number;
};

export type PrepareDrawInstructionDataArgs = { batchSize: number };

export function getPrepareDrawInstructionDataEncoder(): FixedSizeEncoder<PrepareDrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["batchSize", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PREPARE_DRAW_DISCRIMINATOR })
  );
}

export function getPrepareDrawInstructionDataDecoder(): FixedSizeDecoder<PrepareDrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["batchSize", getU32Decoder()],
  ]);
}

export function getPrepareDrawInstructionDataCodec(): FixedSizeCodec<
  PrepareDrawInstructionDataArgs,
  PrepareDrawInstructionData
> {
  return combineCodec(
    getPrepareDrawInstructionDataEncoder(),
    getPrepareDrawInstructionDataDecoder()
  );
}

export type PrepareDrawInput<
  TAccountCrank extends string = string,
  TAccountPool extends string = string,
  TAccountDrawCycle extends string = string,
  TAccountTicketRegistry extends string = string,
> = {
  /** The permissionless signer running the draw preparation. */
  crank: TransactionSigner<TAccountCrank>;
  /** The prize pool state account, validated to be frozen for draw. */
  pool: Address<TAccountPool>;
  /** The current draw cycle account, validated to be awaiting randomness. */
  drawCycle: Address<TAccountDrawCycle>;
  /** The ticket registry loader. */
  ticketRegistry: Address<TAccountTicketRegistry>;
  batchSize: PrepareDrawInstructionDataArgs["batchSize"];
};

export function getPrepareDrawInstruction<
  TAccountCrank extends string,
  TAccountPool extends string,
  TAccountDrawCycle extends string,
  TAccountTicketRegistry extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: PrepareDrawInput<
    TAccountCrank,
    TAccountPool,
    TAccountDrawCycle,
    TAccountTicketRegistry
  >,
  config?: { programAddress?: TProgramAddress }
): PrepareDrawInstruction<
  TProgramAddress,
  TAccountCrank,
  TAccountPool,
  TAccountDrawCycle,
  TAccountTicketRegistry
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    crank: { value: input.crank ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    drawCycle: { value: input.drawCycle ?? null, isWritable: true },
    ticketRegistry: { value: input.ticketRegistry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("crank", accounts.crank),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("drawCycle", accounts.drawCycle),
      getAccountMeta("ticketRegistry", accounts.ticketRegistry),
    ],
    data: getPrepareDrawInstructionDataEncoder().encode(
      args as PrepareDrawInstructionDataArgs
    ),
    programAddress,
  } as PrepareDrawInstruction<
    TProgramAddress,
    TAccountCrank,
    TAccountPool,
    TAccountDrawCycle,
    TAccountTicketRegistry
  >);
}

export type ParsedPrepareDrawInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The permissionless signer running the draw preparation. */
    crank: TAccountMetas[0];
    /** The prize pool state account, validated to be frozen for draw. */
    pool: TAccountMetas[1];
    /** The current draw cycle account, validated to be awaiting randomness. */
    drawCycle: TAccountMetas[2];
    /** The ticket registry loader. */
    ticketRegistry: TAccountMetas[3];
  };
  data: PrepareDrawInstructionData;
};

export function parsePrepareDrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPrepareDrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      crank: getNextAccount(),
      pool: getNextAccount(),
      drawCycle: getNextAccount(),
      ticketRegistry: getNextAccount(),
    },
    data: getPrepareDrawInstructionDataDecoder().decode(instruction.data),
  };
}
