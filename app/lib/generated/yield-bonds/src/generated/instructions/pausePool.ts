const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type WritableAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { findEventAuthorityPda, findGlobalConfigPda } from "../pdas";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  160, 15, 12, 189, 160, 0, 243, 245,
]);

export function getPausePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PAUSE_POOL_DISCRIMINATOR);
}

export type PausePoolInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "CRLD15aDrBh12cNn149dAjaqdV2sWkccFM7y1HKqKZx",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PausePoolInstructionData = { discriminator: ReadonlyUint8Array };

export type PausePoolInstructionDataArgs = {};

export function getPausePoolInstructionDataEncoder(): FixedSizeEncoder<PausePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAUSE_POOL_DISCRIMINATOR })
  );
}

export function getPausePoolInstructionDataDecoder(): FixedSizeDecoder<PausePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPausePoolInstructionDataCodec(): FixedSizeCodec<
  PausePoolInstructionDataArgs,
  PausePoolInstructionData
> {
  return combineCodec(
    getPausePoolInstructionDataEncoder(),
    getPausePoolInstructionDataDecoder()
  );
}

export type PausePoolAsyncInput<
  TAccountGlobalConfig extends string = string,
  TAccountSigner extends string = string,
  TAccountPool extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The global configuration state, used to verify guardian or admin authority. */
  globalConfig?: Address<TAccountGlobalConfig>;
  /** The guardian or admin authority executing the panic pause. */
  signer: TransactionSigner<TAccountSigner>;
  /** The prize pool state account to pause. */
  pool: Address<TAccountPool>;
  eventAuthority?: Address<TAccountEventAuthority>;
  /** The YieldBonds program itself. */
  program?: Address<TAccountProgram>;
};

export async function getPausePoolInstructionAsync<
  TAccountGlobalConfig extends string,
  TAccountSigner extends string,
  TAccountPool extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: PausePoolAsyncInput<
    TAccountGlobalConfig,
    TAccountSigner,
    TAccountPool,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PausePoolInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountSigner,
    TAccountPool,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    signer: { value: input.signer ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await findEventAuthorityPda();
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "CRLD15aDrBh12cNn149dAjaqdV2sWkccFM7y1HKqKZx" as Address<"CRLD15aDrBh12cNn149dAjaqdV2sWkccFM7y1HKqKZx">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPausePoolInstructionDataEncoder().encode({}),
    programAddress,
  } as PausePoolInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountSigner,
    TAccountPool,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PausePoolInput<
  TAccountGlobalConfig extends string = string,
  TAccountSigner extends string = string,
  TAccountPool extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The global configuration state, used to verify guardian or admin authority. */
  globalConfig: Address<TAccountGlobalConfig>;
  /** The guardian or admin authority executing the panic pause. */
  signer: TransactionSigner<TAccountSigner>;
  /** The prize pool state account to pause. */
  pool: Address<TAccountPool>;
  eventAuthority: Address<TAccountEventAuthority>;
  /** The YieldBonds program itself. */
  program?: Address<TAccountProgram>;
};

export function getPausePoolInstruction<
  TAccountGlobalConfig extends string,
  TAccountSigner extends string,
  TAccountPool extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: PausePoolInput<
    TAccountGlobalConfig,
    TAccountSigner,
    TAccountPool,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): PausePoolInstruction<
  TProgramAddress,
  TAccountGlobalConfig,
  TAccountSigner,
  TAccountPool,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    signer: { value: input.signer ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.program.value) {
    accounts.program.value =
      "CRLD15aDrBh12cNn149dAjaqdV2sWkccFM7y1HKqKZx" as Address<"CRLD15aDrBh12cNn149dAjaqdV2sWkccFM7y1HKqKZx">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPausePoolInstructionDataEncoder().encode({}),
    programAddress,
  } as PausePoolInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountSigner,
    TAccountPool,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPausePoolInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The global configuration state, used to verify guardian or admin authority. */
    globalConfig: TAccountMetas[0];
    /** The guardian or admin authority executing the panic pause. */
    signer: TAccountMetas[1];
    /** The prize pool state account to pause. */
    pool: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    /** The YieldBonds program itself. */
    program: TAccountMetas[4];
  };
  data: PausePoolInstructionData;
};

export function parsePausePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPausePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      globalConfig: getNextAccount(),
      signer: getNextAccount(),
      pool: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPausePoolInstructionDataDecoder().decode(instruction.data),
  };
}
