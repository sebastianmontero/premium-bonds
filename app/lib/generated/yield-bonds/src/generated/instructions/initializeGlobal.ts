const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { findGlobalConfigPda } from "../pdas";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_GLOBAL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([47, 225, 15, 112, 86, 51, 190, 231]);

export function getInitializeGlobalDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GLOBAL_DISCRIMINATOR
  );
}

export type InitializeGlobalInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountJobsAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountJobsAccount extends string
        ? ReadonlyAccount<TAccountJobsAccount>
        : TAccountJobsAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGlobalInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeGlobalInstructionDataArgs = {};

export function getInitializeGlobalInstructionDataEncoder(): FixedSizeEncoder<InitializeGlobalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_GLOBAL_DISCRIMINATOR })
  );
}

export function getInitializeGlobalInstructionDataDecoder(): FixedSizeDecoder<InitializeGlobalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeGlobalInstructionDataCodec(): FixedSizeCodec<
  InitializeGlobalInstructionDataArgs,
  InitializeGlobalInstructionData
> {
  return combineCodec(
    getInitializeGlobalInstructionDataEncoder(),
    getInitializeGlobalInstructionDataDecoder()
  );
}

export type InitializeGlobalAsyncInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountJobsAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The global configuration state account.
   *
   * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
   */
  globalConfig?: Address<TAccountGlobalConfig>;
  /** The admin authority initializing the global configuration. */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * exclusively as a reference address for the cranking bot. No data validation
   * or owner checks are performed on it, and it is stored solely in the global configuration.
   */
  jobsAccount: Address<TAccountJobsAccount>;
  /** Solana System Program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeGlobalInstructionAsync<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountJobsAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalAsyncInput<
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountJobsAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeGlobalInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountJobsAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    jobsAccount: { value: input.jobsAccount ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("jobsAccount", accounts.jobsAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeGlobalInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeGlobalInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountJobsAccount,
    TAccountSystemProgram
  >);
}

export type InitializeGlobalInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountJobsAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The global configuration state account.
   *
   * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
   */
  globalConfig: Address<TAccountGlobalConfig>;
  /** The admin authority initializing the global configuration. */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * exclusively as a reference address for the cranking bot. No data validation
   * or owner checks are performed on it, and it is stored solely in the global configuration.
   */
  jobsAccount: Address<TAccountJobsAccount>;
  /** Solana System Program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeGlobalInstruction<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountJobsAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalInput<
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountJobsAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeGlobalInstruction<
  TProgramAddress,
  TAccountGlobalConfig,
  TAccountAdmin,
  TAccountJobsAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    jobsAccount: { value: input.jobsAccount ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("jobsAccount", accounts.jobsAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeGlobalInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeGlobalInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountJobsAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeGlobalInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The global configuration state account.
     *
     * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
     */
    globalConfig: TAccountMetas[0];
    /** The admin authority initializing the global configuration. */
    admin: TAccountMetas[1];
    /**
     * exclusively as a reference address for the cranking bot. No data validation
     * or owner checks are performed on it, and it is stored solely in the global configuration.
     */
    jobsAccount: TAccountMetas[2];
    /** Solana System Program. */
    systemProgram: TAccountMetas[3];
  };
  data: InitializeGlobalInstructionData;
};

export function parseInitializeGlobalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeGlobalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      globalConfig: getNextAccount(),
      admin: getNextAccount(),
      jobsAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeGlobalInstructionDataDecoder().decode(instruction.data),
  };
}
