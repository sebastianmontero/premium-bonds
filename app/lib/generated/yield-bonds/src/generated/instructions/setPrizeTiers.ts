const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { findGlobalConfigPda } from "../pdas";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";
import {
  getPrizeTierDecoder,
  getPrizeTierEncoder,
  type PrizeTier,
  type PrizeTierArgs,
} from "../types";

export const SET_PRIZE_TIERS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [178, 68, 72, 164, 72, 226, 204, 158]
);

export function getSetPrizeTiersDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PRIZE_TIERS_DISCRIMINATOR
  );
}

export type SetPrizeTiersInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      ...TRemainingAccounts,
    ]
  >;

export type SetPrizeTiersInstructionData = {
  discriminator: ReadonlyUint8Array;
  tiers: Array<PrizeTier>;
};

export type SetPrizeTiersInstructionDataArgs = { tiers: Array<PrizeTierArgs> };

export function getSetPrizeTiersInstructionDataEncoder(): Encoder<SetPrizeTiersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tiers", getArrayEncoder(getPrizeTierEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_PRIZE_TIERS_DISCRIMINATOR })
  );
}

export function getSetPrizeTiersInstructionDataDecoder(): Decoder<SetPrizeTiersInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tiers", getArrayDecoder(getPrizeTierDecoder())],
  ]);
}

export function getSetPrizeTiersInstructionDataCodec(): Codec<
  SetPrizeTiersInstructionDataArgs,
  SetPrizeTiersInstructionData
> {
  return combineCodec(
    getSetPrizeTiersInstructionDataEncoder(),
    getSetPrizeTiersInstructionDataDecoder()
  );
}

export type SetPrizeTiersAsyncInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountPool extends string = string,
> = {
  /**
   * The global configuration state, used to verify the admin signature.
   *
   * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
   */
  globalConfig?: Address<TAccountGlobalConfig>;
  /** The admin authority signature authorizing the configuration update. */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * The prize pool state account to update.
   *
   * PDA seeds: `[PRIZE_POOL_SEED, pool.pool_id.to_le_bytes().as_ref()]` (i.e., `b"prize_pool"` + pool_id).
   */
  pool: Address<TAccountPool>;
  tiers: SetPrizeTiersInstructionDataArgs["tiers"];
};

export async function getSetPrizeTiersInstructionAsync<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountPool extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: SetPrizeTiersAsyncInput<
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetPrizeTiersInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pool", accounts.pool),
    ],
    data: getSetPrizeTiersInstructionDataEncoder().encode(
      args as SetPrizeTiersInstructionDataArgs
    ),
    programAddress,
  } as SetPrizeTiersInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool
  >);
}

export type SetPrizeTiersInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountPool extends string = string,
> = {
  /**
   * The global configuration state, used to verify the admin signature.
   *
   * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
   */
  globalConfig: Address<TAccountGlobalConfig>;
  /** The admin authority signature authorizing the configuration update. */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * The prize pool state account to update.
   *
   * PDA seeds: `[PRIZE_POOL_SEED, pool.pool_id.to_le_bytes().as_ref()]` (i.e., `b"prize_pool"` + pool_id).
   */
  pool: Address<TAccountPool>;
  tiers: SetPrizeTiersInstructionDataArgs["tiers"];
};

export function getSetPrizeTiersInstruction<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountPool extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: SetPrizeTiersInput<TAccountGlobalConfig, TAccountAdmin, TAccountPool>,
  config?: { programAddress?: TProgramAddress }
): SetPrizeTiersInstruction<
  TProgramAddress,
  TAccountGlobalConfig,
  TAccountAdmin,
  TAccountPool
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pool", accounts.pool),
    ],
    data: getSetPrizeTiersInstructionDataEncoder().encode(
      args as SetPrizeTiersInstructionDataArgs
    ),
    programAddress,
  } as SetPrizeTiersInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool
  >);
}

export type ParsedSetPrizeTiersInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The global configuration state, used to verify the admin signature.
     *
     * PDA seeds: `[GLOBAL_CONFIG_SEED]` (i.e., `b"global_config"`).
     */
    globalConfig: TAccountMetas[0];
    /** The admin authority signature authorizing the configuration update. */
    admin: TAccountMetas[1];
    /**
     * The prize pool state account to update.
     *
     * PDA seeds: `[PRIZE_POOL_SEED, pool.pool_id.to_le_bytes().as_ref()]` (i.e., `b"prize_pool"` + pool_id).
     */
    pool: TAccountMetas[2];
  };
  data: SetPrizeTiersInstructionData;
};

export function parseSetPrizeTiersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetPrizeTiersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      globalConfig: getNextAccount(),
      admin: getNextAccount(),
      pool: getNextAccount(),
    },
    data: getSetPrizeTiersInstructionDataDecoder().decode(instruction.data),
  };
}
