const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";

export const RESIZE_REGISTRY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [39, 148, 106, 216, 45, 15, 29, 74]
);

export function getResizeRegistryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESIZE_REGISTRY_DISCRIMINATOR
  );
}

export type ResizeRegistryInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountTicketRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountTicketRegistry extends string
        ? WritableAccount<TAccountTicketRegistry>
        : TAccountTicketRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ResizeRegistryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResizeRegistryInstructionDataArgs = {};

export function getResizeRegistryInstructionDataEncoder(): FixedSizeEncoder<ResizeRegistryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESIZE_REGISTRY_DISCRIMINATOR })
  );
}

export function getResizeRegistryInstructionDataDecoder(): FixedSizeDecoder<ResizeRegistryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResizeRegistryInstructionDataCodec(): FixedSizeCodec<
  ResizeRegistryInstructionDataArgs,
  ResizeRegistryInstructionData
> {
  return combineCodec(
    getResizeRegistryInstructionDataEncoder(),
    getResizeRegistryInstructionDataDecoder()
  );
}

export type ResizeRegistryInput<
  TAccountPayer extends string = string,
  TAccountPool extends string = string,
  TAccountTicketRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Account that signs and funds the rent increase for the additional space. */
  payer: TransactionSigner<TAccountPayer>;
  /**
   * The prize pool state account, used to verify that the ticket registry belongs
   * to this pool and that the pool is not currently frozen for drawing.
   *
   * PDA seeds: `[PRIZE_POOL_SEED, pool.pool_id.to_le_bytes().as_ref()]` (i.e., `b"prize_pool"` + pool_id).
   * Bump is verified from the pool's initialized authority bump.
   */
  pool: Address<TAccountPool>;
  /**
   * The zero-copy ticket registry account to be resized.
   * Verified against the prize pool's registry reference.
   * Reallocates additional space (`REGISTRY_REALLOC_STEP`) and charges rent to `payer`.
   */
  ticketRegistry: Address<TAccountTicketRegistry>;
  /** Solana System Program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getResizeRegistryInstruction<
  TAccountPayer extends string,
  TAccountPool extends string,
  TAccountTicketRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: ResizeRegistryInput<
    TAccountPayer,
    TAccountPool,
    TAccountTicketRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ResizeRegistryInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPool,
  TAccountTicketRegistry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: false },
    ticketRegistry: { value: input.ticketRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("ticketRegistry", accounts.ticketRegistry),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getResizeRegistryInstructionDataEncoder().encode({}),
    programAddress,
  } as ResizeRegistryInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPool,
    TAccountTicketRegistry,
    TAccountSystemProgram
  >);
}

export type ParsedResizeRegistryInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Account that signs and funds the rent increase for the additional space. */
    payer: TAccountMetas[0];
    /**
     * The prize pool state account, used to verify that the ticket registry belongs
     * to this pool and that the pool is not currently frozen for drawing.
     *
     * PDA seeds: `[PRIZE_POOL_SEED, pool.pool_id.to_le_bytes().as_ref()]` (i.e., `b"prize_pool"` + pool_id).
     * Bump is verified from the pool's initialized authority bump.
     */
    pool: TAccountMetas[1];
    /**
     * The zero-copy ticket registry account to be resized.
     * Verified against the prize pool's registry reference.
     * Reallocates additional space (`REGISTRY_REALLOC_STEP`) and charges rent to `payer`.
     */
    ticketRegistry: TAccountMetas[2];
    /** Solana System Program. */
    systemProgram: TAccountMetas[3];
  };
  data: ResizeRegistryInstructionData;
};

export function parseResizeRegistryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedResizeRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      pool: getNextAccount(),
      ticketRegistry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getResizeRegistryInstructionDataDecoder().decode(instruction.data),
  };
}
