const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { findGlobalConfigPda } from "../pdas";
import { ANCHOR_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_FORCE_UNLOCK_DRAW_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([68, 52, 69, 37, 202, 238, 252, 24]);

export function getAdminForceUnlockDrawDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_FORCE_UNLOCK_DRAW_DISCRIMINATOR
  );
}

export type AdminForceUnlockDrawInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCurrentDrawCycle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountCurrentDrawCycle extends string
        ? WritableAccount<TAccountCurrentDrawCycle>
        : TAccountCurrentDrawCycle,
      ...TRemainingAccounts,
    ]
  >;

export type AdminForceUnlockDrawInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminForceUnlockDrawInstructionDataArgs = {};

export function getAdminForceUnlockDrawInstructionDataEncoder(): FixedSizeEncoder<AdminForceUnlockDrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ADMIN_FORCE_UNLOCK_DRAW_DISCRIMINATOR,
    })
  );
}

export function getAdminForceUnlockDrawInstructionDataDecoder(): FixedSizeDecoder<AdminForceUnlockDrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminForceUnlockDrawInstructionDataCodec(): FixedSizeCodec<
  AdminForceUnlockDrawInstructionDataArgs,
  AdminForceUnlockDrawInstructionData
> {
  return combineCodec(
    getAdminForceUnlockDrawInstructionDataEncoder(),
    getAdminForceUnlockDrawInstructionDataDecoder()
  );
}

export type AdminForceUnlockDrawAsyncInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountPool extends string = string,
  TAccountCurrentDrawCycle extends string = string,
> = {
  /** The global configuration account, validated to contain the admin address. */
  globalConfig?: Address<TAccountGlobalConfig>;
  /** The admin signer executing the force unlock. */
  admin: TransactionSigner<TAccountAdmin>;
  /** The prize pool state account to unlock. */
  pool: Address<TAccountPool>;
  /** The current draw cycle account, validated to be awaiting randomness. */
  currentDrawCycle: Address<TAccountCurrentDrawCycle>;
};

export async function getAdminForceUnlockDrawInstructionAsync<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountPool extends string,
  TAccountCurrentDrawCycle extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: AdminForceUnlockDrawAsyncInput<
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool,
    TAccountCurrentDrawCycle
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AdminForceUnlockDrawInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool,
    TAccountCurrentDrawCycle
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    currentDrawCycle: {
      value: input.currentDrawCycle ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("currentDrawCycle", accounts.currentDrawCycle),
    ],
    data: getAdminForceUnlockDrawInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminForceUnlockDrawInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool,
    TAccountCurrentDrawCycle
  >);
}

export type AdminForceUnlockDrawInput<
  TAccountGlobalConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountPool extends string = string,
  TAccountCurrentDrawCycle extends string = string,
> = {
  /** The global configuration account, validated to contain the admin address. */
  globalConfig: Address<TAccountGlobalConfig>;
  /** The admin signer executing the force unlock. */
  admin: TransactionSigner<TAccountAdmin>;
  /** The prize pool state account to unlock. */
  pool: Address<TAccountPool>;
  /** The current draw cycle account, validated to be awaiting randomness. */
  currentDrawCycle: Address<TAccountCurrentDrawCycle>;
};

export function getAdminForceUnlockDrawInstruction<
  TAccountGlobalConfig extends string,
  TAccountAdmin extends string,
  TAccountPool extends string,
  TAccountCurrentDrawCycle extends string,
  TProgramAddress extends Address = typeof ANCHOR_PROGRAM_ADDRESS,
>(
  input: AdminForceUnlockDrawInput<
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool,
    TAccountCurrentDrawCycle
  >,
  config?: { programAddress?: TProgramAddress }
): AdminForceUnlockDrawInstruction<
  TProgramAddress,
  TAccountGlobalConfig,
  TAccountAdmin,
  TAccountPool,
  TAccountCurrentDrawCycle
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    currentDrawCycle: {
      value: input.currentDrawCycle ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("currentDrawCycle", accounts.currentDrawCycle),
    ],
    data: getAdminForceUnlockDrawInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminForceUnlockDrawInstruction<
    TProgramAddress,
    TAccountGlobalConfig,
    TAccountAdmin,
    TAccountPool,
    TAccountCurrentDrawCycle
  >);
}

export type ParsedAdminForceUnlockDrawInstruction<
  TProgram extends string = typeof ANCHOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The global configuration account, validated to contain the admin address. */
    globalConfig: TAccountMetas[0];
    /** The admin signer executing the force unlock. */
    admin: TAccountMetas[1];
    /** The prize pool state account to unlock. */
    pool: TAccountMetas[2];
    /** The current draw cycle account, validated to be awaiting randomness. */
    currentDrawCycle: TAccountMetas[3];
  };
  data: AdminForceUnlockDrawInstructionData;
};

export function parseAdminForceUnlockDrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAdminForceUnlockDrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      globalConfig: getNextAccount(),
      admin: getNextAccount(),
      pool: getNextAccount(),
      currentDrawCycle: getNextAccount(),
    },
    data: getAdminForceUnlockDrawInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
