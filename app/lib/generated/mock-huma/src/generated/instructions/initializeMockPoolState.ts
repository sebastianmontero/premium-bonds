const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT = 1200001 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION = 1200002 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE = 1200003 as any;
const SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS = 1200004 as any;
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { type Address } from "@solana/addresses";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array } from "@solana/codecs";
import {
SolanaError } from "@solana/errors";
import {
  type AccountMeta,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type WritableSignerAccount,
} from "@solana/instructions";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  type AccountSignerMeta,
  type TransactionSigner,
} from "@solana/signers";
import { MOCK_HUMA_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_MOCK_POOL_STATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([38, 104, 109, 85, 88, 116, 7, 108]);

export function getInitializeMockPoolStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_MOCK_POOL_STATE_DISCRIMINATOR
  );
}

export type InitializeMockPoolStateInstruction<
  TProgram extends string = typeof MOCK_HUMA_PROGRAM_ADDRESS,
  TAccountPoolState extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoolState extends string
        ? WritableSignerAccount<TAccountPoolState> &
            AccountSignerMeta<TAccountPoolState>
        : TAccountPoolState,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeMockPoolStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeMockPoolStateInstructionDataArgs = {};

export function getInitializeMockPoolStateInstructionDataEncoder(): FixedSizeEncoder<InitializeMockPoolStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_MOCK_POOL_STATE_DISCRIMINATOR,
    })
  );
}

export function getInitializeMockPoolStateInstructionDataDecoder(): FixedSizeDecoder<InitializeMockPoolStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeMockPoolStateInstructionDataCodec(): FixedSizeCodec<
  InitializeMockPoolStateInstructionDataArgs,
  InitializeMockPoolStateInstructionData
> {
  return combineCodec(
    getInitializeMockPoolStateInstructionDataEncoder(),
    getInitializeMockPoolStateInstructionDataDecoder()
  );
}

export type InitializeMockPoolStateInput<
  TAccountPoolState extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poolState: TransactionSigner<TAccountPoolState>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeMockPoolStateInstruction<
  TAccountPoolState extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MOCK_HUMA_PROGRAM_ADDRESS,
>(
  input: InitializeMockPoolStateInput<
    TAccountPoolState,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeMockPoolStateInstruction<
  TProgramAddress,
  TAccountPoolState,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MOCK_HUMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poolState: { value: input.poolState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("poolState", accounts.poolState),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeMockPoolStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeMockPoolStateInstruction<
    TProgramAddress,
    TAccountPoolState,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeMockPoolStateInstruction<
  TProgram extends string = typeof MOCK_HUMA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poolState: TAccountMetas[0];
    payer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeMockPoolStateInstructionData;
};

export function parseInitializeMockPoolStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeMockPoolStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      poolState: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeMockPoolStateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
